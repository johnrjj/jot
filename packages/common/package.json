{
  "name": "@jot/common",
  "version": "1.0.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w --preserveWatchOutput",
    "prettier": "prettier --write 'src/**/*.{js,jsx,ts,tsx,css}'",
    "clean": "rimraf dist"
  },
  "license": "MIT",
  "peerDependencies": {
    "automerge": "^0.9.2"
  },
  "devDependencies": {
    "automerge": "^0.9.2"
  }
}
